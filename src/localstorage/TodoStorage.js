import { memo, useEffect } from "react";

const TodoStorage = memo;

const StoreSection = (e) => {
    const [save, onSave] = usestate([])
    useEffect({
        if(onSave){
            save((data) => !==)
        }
    })
};
